from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(df_breastCancer, test_size=0.33, random_state=42)

X_train

y_train

X_test

y_test


train_test_split(y, shuffle=False)